#!/bin/bash

set -o errexit
set -o nounset

env\
  integration.test.base_url=<%= p("idats.base_url") %>\
  integration.test.uaa_url=<%= p("idats.uaa_url") %>\
  ADMIN_CLIENT_ID=<%= p("idats.admin_client_id") %>\
  ADMIN_CLIENT_SECRET=<%= p("idats.admin_client_secret") %>\
  MAILINATOR_API_KEY=<%= p("idats.mailinator_api_key") %>
  /var/vcap/packages/jdk8/bin/java -jar /var/vcap/packages/dogs/dogs.jar
